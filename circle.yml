machine:
  services:
    - docker

dependencies:
  post:
    - docker info
    - docker build -t ckpt/backend-services:dev .

deployment:
  development:
    branch: dev
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push ckpt/backend-services:dev